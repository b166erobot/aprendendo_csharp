using System;


namespace SoloLearn
{
  class Programa
  {
    static void Main()
    {
      int[] minhaArray = new int[5]; // irá receber 5 elementos do tipo int
      minhaArray[0] = 23;  // o local 0 irá receber o valor 23
      Console.WriteLine(minhaArray[0]);  // imprimindo o valor do local 0
    }
  }
}
